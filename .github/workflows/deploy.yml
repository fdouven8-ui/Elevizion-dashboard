name: Deploy to Replit

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    
    - name: Setup Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '20'
        
    - name: Install dependencies
      run: npm ci
      
    - name: Type check
      run: npm run check
      
    - name: Build
      run: npm run build
      
    - name: Deploy to Replit
      run: |
        echo "🚀 Code pushed to GitHub"
        echo "📦 Ready for Replit deployment"
        echo "🔗 Repository: https://github.com/fdouven8-ui/Elevizion-dashboard"
