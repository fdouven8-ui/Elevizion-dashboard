JE BENT REPLIT AI AGENT.
WE GAAN DE 3 HOOFD-KPI KAARTEN OP HOME AANPASSEN.

DOEL
- KPI kaarten tonen INITIEEL alleen hoofdinfo (titel + hoofdgetal)
- Extra details + CTA’s alleen zichtbaar na uitklappen
- Zelfde UX-principe als Acties & Alerts (klik = meer info)
- Geen backend-wijzigingen nodig

========================
STAP 1 — HERBRUIK UITKLAPLOGICA
========================
Gebruik <details>/<summary> of dezelfde ExpandableCard-logica
die al gebruikt wordt bij “Acties & Alerts”.

Maak (of hergebruik) component:
- ExpandableKpiCard

========================
STAP 2 — COMPONENT (als nieuw nodig)
========================
Maak bestand:
src/components/ui/ExpandableKpiCard.tsx

PLAATS:

```tsx
type Props = {
  title: string;
  icon?: React.ReactNode;
  mainValue: React.ReactNode;
  summary?: React.ReactNode;
  children?: React.ReactNode;
  defaultOpen?: boolean;
};

export function ExpandableKpiCard({
  title,
  icon,
  mainValue,
  summary,
  children,
  defaultOpen = false,
}: Props) {
  return (
    <details
      className="group rounded-2xl border bg-white shadow-sm"
      open={defaultOpen}
    >
      <summary className="list-none cursor-pointer px-5 py-4">
        <div className="flex items-center justify-between">
          <div>
            <p className="text-sm text-slate-600">{title}</p>
            <div className="mt-1 text-3xl font-semibold text-slate-900">
              {mainValue}
            </div>
            {summary && (
              <div className="mt-1 text-sm text-slate-500 group-open:hidden">
                {summary}
              </div>
            )}
          </div>

          <div className="flex items-center gap-2">
            {icon}
            <svg
              className="h-5 w-5 text-slate-400 transition-transform group-open:rotate-180"
              viewBox="0 0 24 24"
              fill="none"
            >
              <path
                d="M7 10l5 5 5-5"
                stroke="currentColor"
                strokeWidth="2"
                strokeLinecap="round"
                strokeLinejoin="round"
              />
            </svg>
          </div>
        </div>
      </summary>

      {children && (
        <div className="px-5 pb-5 pt-1 text-sm text-slate-700">
          {children}
        </div>
      )}
    </details>
  );
}
